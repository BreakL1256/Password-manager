<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Password_manager.SettingsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Settings"
             BackgroundColor="{AppThemeBinding Light=#DED2C3, Dark=#2C2C2C}">

    <ScrollView >
        <VerticalStackLayout Spacing="20" Padding="20">

            <Label Text="Settings" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"
                   TextColor="{AppThemeBinding Light=#50483E, Dark=#F5F5F5}" />

            <Border BackgroundColor="{AppThemeBinding Light=#E9E1D7, Dark=#2C2C2C}" 
                    Padding="15"
                    Stroke="Transparent"
                    StrokeShape="RoundRectangle 5">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Theme" 
                           FontSize="18" 
                           FontAttributes="Bold" 
                           TextColor="{AppThemeBinding Light=#333333, Dark=#FFFFFF}" />

                    <VerticalStackLayout Spacing="10">
                        <Picker x:Name="ThemePicker"
                                SelectedIndexChanged="OnThemeChanged">
                            <Picker.Items>
                                <x:String>System Default</x:String>
                                <x:String>Light Mode</x:String>
                                <x:String>Dark Mode</x:String>
                            </Picker.Items>
                        </Picker>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>

            <Button Text="Close" 
                    Clicked="OnCloseClicked" 
                    BackgroundColor="#50483E" 
                    TextColor="White" 
                    HorizontalOptions="Center" 
                    />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>